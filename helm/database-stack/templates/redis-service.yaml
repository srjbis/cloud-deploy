apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.redis.host }}
  namespace: {{ .Values.namespace }}
  annotations:
    networking.gke.io/load-balancer-type: "Internal"
spec:
  #type: LoadBalancer
  type: ClusterIP
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379