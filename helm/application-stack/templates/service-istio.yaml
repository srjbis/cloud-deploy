apiVersion: v1
kind: Service
metadata:
  name: istio-ingressgateway
  namespace: {{ .Values.namespace }}
spec:
  type: LoadBalancer
  selector:
    istio: ingressgateway
  ports:
  - port: 80
    targetPort: 80